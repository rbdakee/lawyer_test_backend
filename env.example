# Frontend URL для CORS (будет установлен на Render после деплоя фронтенда)
FRONTEND_URL=https://your-frontend.vercel.app

# Python
PYTHONUNBUFFERED=1

# JWT Secret Key для токенов авторизации
JWT_SECRET_KEY=your-secret-jwt-key-change-in-production

# SECRET_LOCAL_TOKEN для защиты API (должен совпадать с frontend)
SECRET_LOCAL_TOKEN=your-secret-local-token-change-in-production

# Firebase credentials (JSON строка или путь к файлу)
# Вариант 1: JSON строка (рекомендуется для продакшена)
FIREBASE_CREDENTIALS={"type":"service_account","project_id":"your-project",...}

# Вариант 2: Путь к файлу с credentials (для локальной разработки)
FIREBASE_CREDENTIALS_PATH=firebase-credentials.json

